#include "Constants.h"
#include "Grid.h"
#include "Graph.h"
#include "Tree.h"
#include "NumTheory.h"
#include "Segtree.h"
#include "CHT.h"

ll N, M, T, arr[MAXN];

void solve() {

}

signed main() {
	cin.tie(0); ios::sync_with_stdio(0);
	//ifstream cin{ ")in.txt" };
	//ofstream cout{ ")out.txt" };

	solve();

	/*
	cin >> T;
	for (int t = 1; t <= T; ++t) {
		cout << "Case #" << t << ": ";
		solve();
		cout << "\n";
	*/
}