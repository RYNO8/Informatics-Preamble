#include "Constants.h"
#include "Grid.h"
#include "Graph.h"
#include "Tree.h"
#include "NumTheory.h"
#include "Segtree.h"
#include "CHT.h"

ll N, M, T, arr[MAXN];

void solve() {

}

signed main() {
	std::cin.tie(0); ios::sync_with_stdio(0);
	//ifstream std::cin{ "_in.txt" };
	//ofstream std::cout{ "_out.txt" };

	solve();

	/*
	std::cin >> T;
	for (int t = 1; t <= T; ++t) {
		std::cout << "Case #" << t << ": ";
		solve();
		std::cout << "\n";
	*/
}